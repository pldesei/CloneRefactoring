(startLine=1921 endLine=1937 srcPath=/home/sonia/NewExperiment/guavaFilter/00220/guava-gwt/src-super/com/google/common/collect/super/com/google/common/collect/Multimaps.java)
    @Override public Iterator<V> iterator() {
      final Iterator<Map.Entry<K, V>> backingIterator =
          multimap().entries().iterator();
      return new Iterator<V>() {
        @Override public boolean hasNext() {
          return backingIterator.hasNext();
        }

        @Override public V next() {
          return backingIterator.next().getValue();
        }

        @Override public void remove() {
          backingIterator.remove();
        }
      };
    }

