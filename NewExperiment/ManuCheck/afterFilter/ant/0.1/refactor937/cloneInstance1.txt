(startLine=1286 endLine=1300 srcPath=/home/sonia/NewExperiment/antFilter/01268/src/main/org/apache/tools/ant/DirectoryScanner.java)
    protected boolean isIncluded(String name) {
        ensureNonPatternSetsReady();

        if (isCaseSensitive()
            ? includeNonPatterns.contains(name)
            : includeNonPatterns.contains(name.toUpperCase())) {
            return true;
        }
        for (int i = 0; i < includePatterns.length; i++) {
            if (includePatterns[i].matchPath(name, isCaseSensitive())) {
                return true;
            }
        }
        return false;
    }

