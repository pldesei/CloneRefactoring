(startLine=714 endLine=725 srcPath=/home/sonia/NewExperiment/antFilter/01293/src/main/org/apache/tools/ant/AntClassLoader.java)
        } else {
            resourceStream = loadResource(name);
            if (resourceStream != null) {
                log("ResourceStream for " + name + " loaded from ant loader", Project.MSG_DEBUG);
            } else {
                resourceStream = loadBaseResource(name);
                if (resourceStream != null) {
                    log("ResourceStream for " + name + " loaded from parent loader",
                            Project.MSG_DEBUG);
                }
            }
        }

