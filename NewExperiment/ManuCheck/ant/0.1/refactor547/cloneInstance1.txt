(startLine=472 endLine=484 srcPath=/home/sonia/NewExperiment/antFilter/00943/src/main/org/apache/tools/ant/taskdefs/ExecTask.java)
            if (out != null)  {
                try {
                    outputStream 
                        = new FileOutputStream(out.getAbsolutePath(), append);
                    log("Output redirected to " + out, Project.MSG_VERBOSE);
                } catch (FileNotFoundException fne) {
                    throw new BuildException("Cannot write to " + out, fne,
                                             getLocation());
                } catch (IOException ioe) {
                    throw new BuildException("Cannot write to " + out, ioe,
                                             getLocation());
                }
            }

