(startLine=231 endLine=240 srcPath=/home/ubuntu/luceneFilter/1419/lucene/src/test/org/apache/lucene/analysis/TestTeeTokenFilter.java)
    public Token next(final Token reusableToken) throws IOException {
      Token nextToken = null;
      for (nextToken = input.next(reusableToken);
           nextToken != null && count % modCount != 0;
           nextToken = input.next(reusableToken)) {
        count++;
      }
      count++;
      return nextToken;
    }

