(startLine=42 endLine=52 srcPath=/root/Projects/elasticsearchFilter/1636/elasticsearch/modules/benchmark/micro/src/main/java/org/elasticsearch/benchmark/checksum/ChecksumBenchmarkTest.java)
    private static void crc(long dataSize) {
        long start = System.currentTimeMillis();
        CRC32 crc = new CRC32();
        byte[] data = new byte[BATCH_SIZE];
        long iter = dataSize / BATCH_SIZE;
        for (long i = 0; i < iter; i++) {
            crc.update(data);
        }
        crc.getValue();
        System.out.println("CRC took " + new TimeValue(System.currentTimeMillis() - start));
    }

