(startLine=39 endLine=49 srcPath=/home/ubuntu/guavaFilter/989/guava/guava-tests/test/com/google/common/io/MultiReaderTest.java)
      public Reader getInput() throws IOException {
        if (counter[0]++ != 0) {
          throw new IllegalStateException("More than one supplier open");
        }
        return new FilterReader(supplier.getInput()) {
          @Override public void close() throws IOException {
            super.close();
            counter[0]--;
          }
        };
      }

