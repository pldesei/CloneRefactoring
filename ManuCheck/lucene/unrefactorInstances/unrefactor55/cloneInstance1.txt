(startLine=77 endLine=85 srcPath=/home/ubuntu/luceneFilter/20/lucene/lucene/test-framework/src/java/org/apache/lucene/codecs/asserting/AssertingDocValuesFormat.java)
    public void addNumericField(FieldInfo field, Iterable<Number> values) throws IOException {
      int count = 0;
      for (Number v : values) {
        count++;
      }
      assert count == maxDoc;
      checkIterator(values.iterator(), maxDoc, true);
      in.addNumericField(field, values);
    }

