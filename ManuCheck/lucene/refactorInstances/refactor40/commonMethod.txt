(startLine=120 endLine=127 srcPath=/home/ubuntu/luceneFilter/534/lucene/lucene/core/src/java/org/apache/lucene/util/packed/PackedInts.java)
    public static Format byId(int id) {
      for (Format format : Format.values()) {
        if (format.getId() == id) {
          return format;
        }
      }
      throw new IllegalArgumentException("Unknown format id: " + id);
    }

